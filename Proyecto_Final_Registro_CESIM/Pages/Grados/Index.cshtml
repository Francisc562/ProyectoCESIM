@page
@model Proyecto_Final_Registro_CESIM.Pages.Grados.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Grado[0].Nivel)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Grado[0].docente)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Grado) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nivel)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.docente.docenteID)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.gradoID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.gradoID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.gradoID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
